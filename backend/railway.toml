[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npm run start:prod"
healthcheckPath = "/api/v1/health" # Assuming health check might be added or used
healthcheckTimeout = 100
restartPolicyType = "on_failure"
